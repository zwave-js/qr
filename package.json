{
  "name": "zwave-qr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/script.ts --bundle --outdir=build --minify --sourcemap --target=es2022",
    "serve": "node .dev.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.20.2",
    "qrcode-svg": "^1.1.0"
  }
}
