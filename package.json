{
  "name": "zwave-qr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/script.ts --bundle --format=esm --outdir=build --sourcemap --target=es2022 --platform=browser --analyze=verbose",
    "serve": "node .dev.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@zwave-js/core": "^14.1.0",
    "esbuild": "^0.24.0",
    "qrcode-svg": "^1.1.0"
  }
}
